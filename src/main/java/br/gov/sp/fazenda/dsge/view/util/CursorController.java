/*
 * Decompiled with CFR 0_122.
 * 
 * Could not load the following classes:
 *  br.gov.sp.fazenda.dsge.view.table.PageableSortableTableActionListener
 *  br.gov.sp.fazenda.dsge.view.util.CursorController
 *  br.gov.sp.fazenda.dsge.view.util.CursorController$1
 *  br.gov.sp.fazenda.dsge.view.util.CursorController$2
 *  br.gov.sp.fazenda.dsge.view.util.CursorController$3
 */
package br.gov.sp.fazenda.dsge.view.util;

import br.gov.sp.fazenda.dsge.view.table.PageableSortableTableActionListener;
import br.gov.sp.fazenda.dsge.view.util.CursorController;
import java.awt.Component;
import java.awt.Cursor;
import java.awt.event.ActionListener;
import java.beans.PropertyChangeListener;

/*
 * Duplicate member names - consider using --renamedupmembers true
 */
public class CursorController {
    private static final Cursor a = new Cursor(3);
    private static final Cursor b = new Cursor(0);
    private static final int a = 500;

    private CursorController() {
    }

    public static ActionListener createActionListener(Component component, ActionListener mainActionListener) {
         actionListener = new /* Unavailable Anonymous Inner Class!! */;
        return actionListener;
    }

    public static PropertyChangeListener createPropertyChangeListener(Component component, PropertyChangeListener mainActionListener) {
         pCListener = new /* Unavailable Anonymous Inner Class!! */;
        return pCListener;
    }

    public static PageableSortableTableActionListener createPageableSortableTableActionListener(Component component, PageableSortableTableActionListener mainActionListener) {
         pCListener = new /* Unavailable Anonymous Inner Class!! */;
        return pCListener;
    }

    static /* synthetic */ Cursor a() {
        return a;
    }

    static /* synthetic */ Cursor b() {
        return b;
    }
}

